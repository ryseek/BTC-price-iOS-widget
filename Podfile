# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

use_frameworks!

target 'BTCwidget' do
  # Comment the next line if you're not using 


	pod 'Alamofire', '~> 4.5' 

  target 'Crypto Today' do
  	pod 'Alamofire', '~> 4.5' 
    # Because we only need Realm in our extension
  end
end



